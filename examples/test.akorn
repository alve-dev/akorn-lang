// Asignacion de tipo imcompatible
let int a = "5";
let float b = true;
let bool c = 1;

// Conversion implicita prohibida
let int d = 5.0;
let bool e = 0;

// Operador invalida para el tipo
let int f = -"5";
let int g = not 3;
let bool h = 5 + true;

// Operaciones con tipos incompatibles
let int i = 3 + 5.0;
let float j = 10 % 3.2;
let bool k = 5 < "hola";

// Uso de operadores aritmeticos en bool
let bool l = true + false;
let bool m = false * true;

// Uso antes de declaracion
let int n = non_exist + 5;

// Usos de variables existentes pero vacias
let int o;
let int p = o + 5;

// variable en otra scope
if true {
    let int x = 5;
}
let int y = x;

// multi-declaration con expresion invalida en una de las vars
let int w = 5, z = 3.2;

// multi-declaration con error circular, como ñ no existe el intento de declaracion de u da error y no se guarda entonces ñ tambien da error porque u no existe
let int u = ñ, ñ = u; 



