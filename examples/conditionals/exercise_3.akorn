//Conditionals - Exercise 3: Comprobacion de 0 en una division

let int a = readInt("Digite un número -> "), b = readInt("Digite otro número -> ");

if b == 0
{
    writeline("No se puede dividir por 0");
}
else
{
    writeline(a, '/', b, '=', a / b);
}