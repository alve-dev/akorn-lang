/*Pedir un numero entero y evaluar si es negativo, positivo o cero
var int x = readInt("Digita un numero: ");

if (x > 0)
{
    write("El numero es positivo");
}
elif (x < 0)
{
    write("El numero es negativo");
}
else
{
    write("El numero cero");
}*/

/*Pide un numero entero y comprueba su rango
var int number = readInt("Digita un numero del 1 al 100: ");

if (number >= 90)
{
    write("Excelente");
}
elif (number >= 70)
{
    write("Aprobado");
}
elif (number >= 50)
{
    write("Recuperacion");
}
else
{
    write("Reprobado");
}*/

/*
var int age = readInt("Digita tu edad: ");
var bool tiene_dolor = readBool("Te duele algo?[y/n]: ", 'y', 'n');
//readBool(prompt, true, false) -> bool, es temporal asi que es imperfecta por que esta en el interprete temporalmente luego ya lo hare pas profesional sino es para pruebas rapidas del lenguaje asi como write y los otros read functions

if (age > 65 or tiene_dolor)
{
    write("Atencion prioritaria");
}
else
{
    write("Espera en sala");
}*/

/*var bool exist = readBool("El archivo existe?[y/n]: ", 'y', 'n');
var int len_file = readInt("Cual es el tamaño del archivo?: ");

if (not exist)
{
    write("Archivo creado");
}
else
{
    if (len_file == 0)
    {
        write("Archivo vacio");
    }
    elif (len_file > 0)
    {
        write("Archivo con datos");
    }
}*/

/*
let int x = 10;

if (x > 5)
{
    write("1");
}
elif (x > 2)
{
    write("2");
}*/