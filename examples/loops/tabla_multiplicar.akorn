// hacer un script que tome un numero y muestre su tabla de multiplicar del 1..10

let int number = readInt("Digita un numero: ");

var int i = 1;
while i <= 10
{
    writeline(number, "*", i, "=", number * i);
    i += 1;
}